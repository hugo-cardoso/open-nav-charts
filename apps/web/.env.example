# URL base da API REST consumida pela aplicação. Sem barra final.
# Em desenvolvimento aponta para a API local de apps/api.
# As variáveis são lidas no momento do build: alterá-las exige reiniciar o Vite.
VITE_API_BASE_URL=http://localhost:3000
